import Foundation

// MARK: - Shared AnyCodable Type
// This is a re-export of the AnyCodable type from the parent QuotaBackend module
// to make it accessible within the ClaudeProxy submodule.

// Since we're in the same module (QuotaBackend), we can directly use AnyCodable
// without importing. This file serves as documentation and a central reference point.

// AnyCodable is defined in QuotaBackend/Sources/QuotaBackend/ProviderProtocol.swift
// and is available throughout the QuotaBackend module.
